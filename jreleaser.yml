project:
  name: tool
  version: 2026.07.19
  versionPattern: CHRONVER
  languages:
    java:
      groupId: dev.mccue

signing:
  active: ALWAYS
  armored: true

deploy:
  maven:
    mavenCentral:
      release-deploy:
        active: ALWAYS
        url: https://central.sonatype.com/api/v1/publisher
        stagingRepositories:
          - build/jstage